---
title: "WP-E1-009 Handoff"
document_type: "work-packet-handoff"
status: "current"
version: "0.1.0"
created: "2026-05-24"
updated: "2026-05-24"
owner: "Monad Project"
epic: "E1"
work_packet: "WP-E1-009"
tags:

* handoff
* rust
* runtime-foundation
* repository-contract

---

# WP-E1-009 Handoff

## Work Packet

WP-E1-009 — Establish Repository Contract Check Foundation

## Current Epic

E1 — Runtime Foundation

## Prior Work

E0 — Project Foundation is complete.

WP-E1-001 through WP-E1-008 are complete.

## Objective

Add the first repository-contract check foundation.

## Repository Contract

This slice adds:

* `crates/monad-core/src/repo_contract.rs`;
* `RequiredPathKind`;
* `RequiredPath`;
* `RepositoryContract`;
* `check_repository_contract`;
* repository-contract integration into `run_workspace_checks`;
* repository-contract diagnostics in `monad check`.

## Next Expected Slice

After WP-E1-009, proceed to runtime output formatting or repository contract hardening.

## Rules

* Use Rust Apprenticeship Mode.
* Provide complete file contents.
* Add beginner-readable comments.
* Keep CLI thin.
* Put durable runtime logic in `monad-core`.
* Add tests.
* Run verification.
* Use `python3`, not `python`.
* Do not introduce Bazel, Pants, Buck2, or Nx.

## Expected Verification

```bash
cargo fmt --check
cargo test
cargo run --quiet -p monad-cli -- check
tools/scripts/verify.sh
```

