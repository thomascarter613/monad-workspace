---
title: "WP-E1-011 Handoff"
document_type: "work-packet-handoff"
status: "current"
version: "0.1.0"
created: "2026-05-24"
updated: "2026-05-24"
owner: "Monad Project"
epic: "E1"
work_packet: "WP-E1-011"
tags:

* handoff
* rust
* runtime-foundation
* output-format-argument

---

# WP-E1-011 Handoff

## Work Packet

WP-E1-011 — Establish CLI Output Format Argument Foundation

## Current Epic

E1 — Runtime Foundation

## Prior Work

E0 — Project Foundation is complete.

WP-E1-001 through WP-E1-010 are complete.

## Objective

Add output-format argument parsing to the CLI.

## Output Format Argument

This slice adds:

* `CliInvocation`;
* `--format text`;
* `--format=text`;
* output format parsing before or after commands;
* missing format value errors;
* unsupported format errors;
* multiple command detection;
* CLI tests.

## Next Expected Slice

After WP-E1-011, proceed to JSON output formatting or repository contract hardening.

## Rules

* Use Rust Apprenticeship Mode.
* Provide complete file contents.
* Add beginner-readable comments.
* Keep CLI thin.
* Put durable runtime logic in `monad-core`.
* Add tests.
* Run verification.
* Use `python3`, not `python`.
* Do not introduce Bazel, Pants, Buck2, or Nx.

## Expected Verification

```bash id="lgz7wb"
cargo fmt --check
cargo test
cargo run --quiet -p monad-cli -- info --format text
cargo run --quiet -p monad-cli -- check --format=text
tools/scripts/verify.sh
```

